import { createUseStyles } from 'react-jss'

export default createUseStyles({
  app: {
    display: 'grid',
    gridRow: '1fr 1fr',
    width: '100%',
    height: '100%',
    justifyContent: 'center',
    alignItems: 'center'
  }
})